const mainPokemon_URL = "https://pokeapi.co/api/v2/pokemon/"; //Das ist die Haupt-URL
let allPokemonData = [];//Array zum speichern aller Daten



async function showPokeCard(){
    let pokeCard = document.getElementById('poke-card');
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }

}